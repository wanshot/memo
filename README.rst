memo
===============

Installation
---------------------

::

   # init memo
   $ memo init
   # edit memorc
   $ vi /Your/home/directory/.memo.d/memorc


Usage
-------------

::

   # new memo file
   $ memo new

   # show memo files
   $ memo list

   # grep memo files
   $ memo grep <pattern>
